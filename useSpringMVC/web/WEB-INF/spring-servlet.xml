<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">
    <!-- Scans the classpath of this application for @Components to deploy as beans -->
    <!--使用注解的包，包括子集-->
    <context:component-scan base-package="web.kwong.test" />

    <!--&lt;!&ndash; HandlerMapping &ndash;&gt;-->
    <!--<bean-->
            <!--class="org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping" />-->

    <!--&lt;!&ndash; HandlerAdapter &ndash;&gt;-->
    <!--<bean-->
            <!--class="org.springframework.web.servlet.mvc.SimpleControllerHandlerAdapter" />-->

    <!-- Configures the @Controller programming model -->
    <!-- 启用注解扫描包 -->
    <mvc:annotation-driven />

    <mvc:default-servlet-handler/>
    <!-- Resolves view names to protected .jsp resources within the /WEB-INF/views directory -->
    <!--视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>
</beans>

        <!--这段的大概意思是, 自动扫描包”web.kwong.test”来找到controller定义. 同时, controller都使用@Controller来定义.-->
        <!--bean里的定义, 表明所有的View文件都到/WEB-INF/views目录来找, 后缀都为.jsp.-->